package cv.dge.dge_api_intermed_lab.application.perfilcandidato.dto;

public record CandidaturaPorTipoResponse(
        String tipoOferta,
        String tipoOfertaDescricao,
        Long total
) {
}
